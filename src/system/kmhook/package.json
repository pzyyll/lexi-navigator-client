{
  "name": "kmhook",
  "dependencies": {
    "node-addon-api": "^8.0.0"
  },
  "scripts": {
    "test": "node test.js",
    "rebuild": "node-gyp rebuild",
    "build": "node-gyp clean && node-gyp configure && node-gyp build"
  }
}
